import { 
    useState // 私有状态
 } from 'react'

const TodoForm = () => {
    // JSX一定得有唯一的最外层元素 树来编译解析JSX，生成虚拟DOM树
    return (
       <>
        <h1 className="header">TodoList</h1>
        <form>
            
        </form>
       </>
    )
}

export default TodoForm
